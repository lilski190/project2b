/**
 * Layout.js
 * Layout-Komponente für die Anwendung.
 * Die Komponente kommt von einem Default Setup Projekt das ich für Next.js Projektes mit Supabase aufgesetzt habe.
 */
export default function Layout({ children }) {
  return <>{children}</>;
}
